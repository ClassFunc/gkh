import {ai} from "@/ai/ai";
import {z} from "genkit";

{{commandInputDeclarationCode}}

const {{name}}Tool = ai.defineTool(
{
    name: '{{name}}Tool',
    description: $description,
    inputSchema: z.any(),
    outputSchema: z.any(),
}, async (input) => {
    //implementation

    }
);
